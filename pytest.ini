[pytest]
testpaths =
    phase2_qaoa/tests
addopts = --import-mode=importlib -p no:cacheprovider
norecursedirs =
    .git
    .venv
    .python_packages
    handoffs
    outputs
    paper
    pytest-cache-files-*
